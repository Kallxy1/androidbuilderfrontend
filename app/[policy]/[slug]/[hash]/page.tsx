import Link from "next/link";

const titles: Record<string, string> = { privacy: "Privacy Policy", terms: "Terms of Service", security: "Security Policy", service: "Service Information", about: "About BuildBox" };
export default async function SluggedPolicyPage({ params }: { params: Promise<{ policy: string; slug: string; hash: string }> }) { const { policy, slug, hash } = await params; const title = titles[policy] || "BuildBox Information"; return <main className="mx-auto max-w-2xl px-5 py-12"><Link href="/" className="text-sm text-muted-foreground">← Kembali</Link><div className="card mt-8 p-6 sm:p-8"><div className="text-xs uppercase tracking-widest text-muted-foreground">/{policy}/{slug}/{hash}</div><h1 className="mt-3 text-3xl font-black">{title}</h1><p className="mt-4 leading-7 text-muted-foreground">Halaman ini memakai slug dan hash agar link dapat dibuat spesifik, dilacak masa berlakunya, dan di-expire oleh server.</p><div className="mt-6 rounded-xl bg-muted p-4 text-sm leading-6 text-muted-foreground">Link hash: <span className="font-mono">{hash}</span><br />Slug: <span className="font-mono">{slug}</span><br />Expiration akan divalidasi setelah token service dan storage diaktifkan.</div></div></main>; }
