import Link from "next/link";

const titles: Record<string, string> = { terms: "Terms of Service", privacy: "Privacy Policy", security: "Security Policy", service: "Service Information" };
export default async function LegalSlugPage({ params }: { params: Promise<{ section: string; slug: string; hash: string }> }) { const { section, slug, hash } = await params; return <main className="mx-auto max-w-2xl px-5 py-12"><Link href="/" className="text-sm text-muted-foreground">← Kembali</Link><div className="card mt-8 p-6 sm:p-8"><div className="text-xs uppercase tracking-widest text-muted-foreground">legal/{section}/{slug}/{hash}</div><h1 className="mt-3 text-3xl font-black">{titles[section] || "BuildBox Legal"}</h1><div className="mt-6 space-y-4 leading-7 text-muted-foreground"><p>BuildBox menggunakan link slug dan hash untuk membedakan halaman, membuat link dapat expired, dan menyiapkan validasi token di server.</p><p>Slug: <span className="font-mono">{slug}</span></p><p>Hash: <span className="font-mono">{hash}</span></p><p className="rounded-xl bg-muted p-4 text-sm">Expiration dan signature akan diverifikasi server setelah access-token storage diaktifkan.</p></div></div></main>; }
